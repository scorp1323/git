1 - запустити денвер або SQL локальний
2 - запустити командний р€док в≥д адм≥н≥стратора cmd
3 - C:\dnw\usr\local\mysql-5.7\bin\mysql.exe -u root -p    - набрати в командному р€дку
4 - запросить пароль Enter password: - просто нажати ентер - пароль пустий
5 - видасть Welcome to the MySQL monitor.   - запрошенн€    mysql>
6 - вводим   show databases;  - покаже вс≥ на€вн≥ Ѕƒ
7 - use lapous; show tables; - вибрати Ѕƒ-lapous та перегл€нути список таблиць в н≥й
7 - SHOW COLUMNS FROM users; - просмотр колонок и их свойств у таб.users

 SHOW COLUMNS FROM user;
+-------------+-------------+------+-----+---------+----------------+
| Field       | Type        | Null | Key | Default | Extra          |
+-------------+-------------+------+-----+---------+----------------+
| id          | int(6)      | NO   | PRI | NULL    | auto_increment |
| login       | varchar(50) | NO   | UNI | NULL    |                |
| password    | varchar(50) | NO   |     | NULL    |                |
| first_name  | varchar(50) | NO   |     | NULL    |                |
| second_name | varchar(50) | NO   |     | NULL    |                |
+-------------+-------------+------+-----+---------+----------------+

—писок основних команд SQL:
- CREATE DATABASE DB_NAME; Ч создать новую бд;
- SHOW DATABASES; Ч просмотр имеющихс€ бд;
- USE DB_NAME; Ч выбрать бд;
- SHOW TABLES; Ч просмотр всех таблиц;
- SHOW COLUMNS FROM TABLE_NAME; Ч просмотр колонок и их свойств;
- CREATE TABLE TABLE_NAME (`ID` INT(11),`NAME` VARCHAR(255)); Ч создание таблицы;
- ALTER TABLE TABLE_NAME ADD COLUMN_NAME INT(11); Ч добавить колонку в таблицу;
- ALTER TABLE TABLE_NAME DROP COLUMN_NAME; Ч удалить колонку из таблицы;
- MYSQL -UUSERNAME -PPASSWORD -HHOST DB_NAME < FILE_NAME.SQL Ч залить бд;
- MYSQLDUMP -UUSERNAME -PPASSWORD -HHOST DB_NAME > FILE_NAME.SQL Ч сделать дамп бд;
- QUIT; Ч выход из консольки MySQL.

- ALTER DATABASE mydbnew COLLATE utf8_general_ci; - перекодуванн€ бази
- ALTER TABLE users CONVERT TO CHARACTER SET utf8 COLLATE utf8_general_ci;- перекодуванн€ таб.
- ALTER TABLE `mydbnew`.`users` CONVERT TO CHARACTER SET utf8 COLLATE utf8_general_ci; - перекодуванн€ таб.
- SHOW VARIABLES; - перев≥рити встановлену кодировку бази

- show table status; - працюЇ так само, €к SHOW TABLES, але надаЇ багато ≥нфи
- RENAME TABLE user TO users; - перейменуванн€ таблиц≥

- ALTER TABLE users ADD PRIMARY KEY (ID); - зм≥нити стовпець ID, зробивши його ун≥кальним
- ALTER TABLE users MODIFY COLUMN login VARCHAR(50) CHARACTER SET utf8 COLLATE utf8_general_ci NOT NULL; - перекодуванн€ стовпц€
- ALTER TABLE users MODIFY ID INT(6) AUTO_INCREMENT;- зм≥нити стовпець ID - AUTO_INCREMENT
- ALTER TABLE `users` CHANGE COLUMN `ID` `IDI` int(6) NOT NULL AUTO_INCREMENT; - перейменувати ID
- ALTER TABLE users MODIFY ID INT(6) NOT NULL; - зм≥нити стовпець ID - NOT NULL
- ALTER TABLE users ADD UNIQUE (login);

- ALTER TABLE users 
MODIFY password VARCHAR(50) NOT NULL,
MODIFY first_name VARCHAR(50) NOT NULL,
MODIFY second_name VARCHAR(50) NOT NULL; - зм≥нити к≥лька стовп≥в - NOT NULL

 - INSERT INTO users (login,password,first_name,second_name) VALUES ('scorp1323','131313','—ерг≥й', '“аранов');
 - SELECT * FROM users;

- UPDATE users SET first_name = '—ерг?й' WHERE 'first_name' = '—ерг?й'; - —ерг?й ->  